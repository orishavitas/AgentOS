# {{PROJECT_NAME}}

## Goal
{{PROJECT_GOAL}}

## Repo map
- `docs/` — PRD, roadmap, architecture, QA plan
- `frontend/` — UI
- `backend/` — API and business logic
- `shared/` — shared contracts/types
- `design/` — lightweight design artifacts (optional)
- `tests/` — automated tests and fixtures

## Quick start
1. Prereqs:
   - (fill in)
2. Install:
   - (fill in)
3. Run:
   - (fill in)
4. Test:
   - (fill in)

